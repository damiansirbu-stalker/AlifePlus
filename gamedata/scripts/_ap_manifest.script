--- _ap_manifest: AlifePlus identity. Pure data, no logic. Read by _ap_init (the gate) and the validator (deps.manifest_structure).

MANIFEST = {
    name    = "AlifePlus",
    version = "1.8.8-snapshot",
    xlibs   = "1.8.5",
}
