next
  Summary: PDA news system (English and Russian), mutant infestation with alpha gate, species-pure mutant spawns, additive territory conquest, personality ceiling retune
  Gameplay:
    Added: PDA news system. Squad activity surfaces as radio chatter from an online stalker on your frequency. News carries information the simulation runs on (where to go, where to avoid, what is fresh, who is active, what is the threat). NPCs already act on these events; the PDA is the player's window into the same information stream. Templates render as a stalker on the radio relaying half-known news with hedge leads ("Heard...", "Word is...", "From what I'm picking up..."). Past tense throughout, periods between clauses, full sentences with verbs.
    Added: paired news. One event that fires two sibling consequences ships as a single line instead of two messages. Covers massacre, squadkill, basekill, wounded, harvest.
    Added: news routes through Anomaly's dynamic_news_manager. Integrates with vanilla rules: emission and psi-storm suppression (queues clear during the event), PDA-charged gate (no news when the apparatus is dead or out of power), shared 3-message queue cap. Cold-start fallback to direct PDA delivery during the manager's ~27 second initialization window.
    Added: canon channel routing. The speaker's faction is the radio channel. Monolith, Army, Greh, and ISG channels stay member-only — non-members never hear that internal chatter, matching vanilla and every A/S-tier news mod.
    Added: news scope toggle (MCM, default Allies). Own: only your faction speaks. Allies: your faction plus factions friendly to you. World: any non-enemy stalker.
    Added: news age window (MCM, default 12 game hours). Each compose tick picks a random event from the past N game hours, so messages feel like gossip pulled from recent memory instead of a sequential log.
    Added: mutant infestation. Feral, predator, and aberrant mutants claim smart terrains as nests. Original spawns are replaced by the infesting species. Only squads carrying an alpha can infest. Per-level cap (MCM 1-5, default 1).
    Changed: territory conquest is additive. Conquerors add spawns alongside originals instead of replacing them.
    Changed: species-pure spawns on mutant-claimed smart terrains. Each nest spawns one species instead of mixed packs. Stalker conquest unchanged.
    Changed: personality ceiling retuned from 0.80 to 0.70 across every consequence. Floor stays at 0.20.
    Changed: stash fill skips squads at bases. Matches stash loot and stash ambush behavior.
  Core:
    Added: spawn attribution tracing. When DEBUG log level is enabled, every squad spawned by area_conquer or instinct_infest is logged with smart, faction, and section. Filters on the AP-injected respawn_params section, so coexisting LTX-entry spawns on the same smart do not produce false positives.
    Added: activity record now stores translated display strings for faction, name, and species (subject and other) at capture time. News, HUD markers, and external integrators read pre-resolved strings; squads that die or unregister between capture and render still display correctly.
    Changed: stash cause checks at_base before publishing. The single check in the cause predicate replaces three identical checks at the consequence level (loot, ambush, fill) and runs before personality so probability rolls aren't wasted on at-base squads.
    Changed: PDA map markers redesigned with multi-line label. Action phrase, "Subject: name (faction)", and "Other: name (faction)" for two-party consequences. Reads pre-translated fields directly from the activity record.
  Localization:
    Added: PDA news ships in English and Russian. Stalker register throughout, loners talk like loners.
    Added: PDA map marker activity labels in English and Russian.
    Added: mutant species names in news (bloodsuckers, chimeras, lurkers, psy dogs) instead of generic "monsters".
    Fixed: Russian MCM News tab strings synced with English.
  MCM:
    Added: News tab with master toggle and compose interval sliders (1-600 seconds, default 30-100).
    Added: News scope (own / allies / world, default allies).
    Added: News max age slider (1-72 game hours, default 12).
    Added: instinct infest cause and consequence settings (enabled, threshold, rush, personality floor and ceiling).
    Added: infestation mutator settings (enabled, max per level, spawn count, decay hours).
    Removed: per-consequence PDA chance sliders. News volume is tuned in the News tab.
  Integration:
    Activity record. Write via ap_core_broker.record(squad_id, cause, consequence, opts). Read via get_record(opts) (O(1) for { squad_id, assigned = true }) or get_records(opts) for bulk. clear_record(squad_id) on entity death.
    Localized rendering. ap_core_const.CONSEQUENCE_INFO[consequence] returns { name_key, action_key }: name_key resolves to a short caption ("Massacre Investigate"), action_key to the full action phrase ("Investigating a Massacre Site"). Both XML ids; resolve via game.translate_string. EN and RU ship.
    Trace paths. ap_core_const.CONSEQUENCE_PHASE supplies sub-phase identifiers for ap_core_debug.observe (FIND_TARGETS, MOVE_SQUAD, ARRIVE, ...) at DEBUG log level.
    News dispatch. ap_ext_news.record_event(subject_squad, cause, consequence, opts). opts.cause_id groups siblings for paired dispatch; opts.other_squad enables two-party templates. Template pools follow per-consequence, paired, and pattern id conventions.
    Spawn injection. xsmart.set_shared_spawn(smart, key, identity, num) adds a respawn entry alongside originals (conquest pattern). xsmart.set_exclusive_spawn(smart, key, identity, num) replaces them (infest pattern). Identity accepts stalker factions and mutant species.
    xlibs helpers. xsquad.get_commander_name, xcreature.get_mutant_species, xlevel.get_level_name, xstring.capitalize_first, xttltable fifo_cache :each + on_evict.
  Fixed:
    Fixed: squadkill_revenge chased the player after the player's actor_stalker relation drifted hostile from own-faction kills.
    Fixed: news composer dropped every entry. Eligibility filter compared numeric entry.level_id against level.current() (level name string). Replaced with xlevel.get_actor_level_id() (numeric).
    Fixed: news speaker pool always empty. actor:character_community() returned actor_<faction> (not present in game_relations tables), so scope filter rejected every NPC. Replaced with get_actor_true_community().
    Removed: runtime per-sentence capitalize in _clean_output. Slot values are capitalized in XML; runtime fixup was overcorrecting and added work per dispatch.
  Templates:
    Rewrote the entire English news corpus (36 consequence pools + 5 pair pools, 5 variants each) on a documented discipline: each variant carries an explicit consequence tell, pair pools tag both halves and the cause, speaker rotation across hearsay/witness/survivor/tracker/named, length variation per pool, no parallel-rhythm halves, no stage-dressing lists, no war-correspondent vocabulary, doomer through fact selection. See doc/standards/writing-standards.md "Generative templates" section for the full rule set.
    Russian corpus reset to clone of English pending translation.

1.4.0
  Summary: Mutant instincts, pipeline redesign, day/night cycle, food chain, range-based perception, personality recalibration, statistics overlay, integration API
  Gameplay:
    Added: mutant instincts. Five deprivation-scored drives (scatter, feed, sleep, explore, socialize) parallel to stalker needs. Per-instinct xbus routing, Hull drive competition, independent thresholds.
    Changed: mutant personality and alignment redesigned from GSC source (monstry.doc). Species and subspecies detection via xlibs replaces naive community categories.
    Added: food chain. Four behavioral groups (cowardly, feral, predator, aberrant) with tiered avoidance. Cowardly flees feral+predator+aberrant, feral flees predator+aberrant, predator flees aberrant.
    Added: day/night cycle. Stalkers work by day, rest at night. Nocturnal mutants (bloodsuckers, lurkers, chimeras, zombies, fractures) hunt at night, retreat at dawn. All needs and instincts time-gated accordingly.
    Added: range-based perception. EyeRange (200m, vision), RadioRange (500m, stalker PDA), ScentRange (500m, mutant tracking). Each cause type maps to a range tier. Distances derived from empirical smart terrain geometry analysis across 14 levels (nearest-neighbor spacing, vertex density, level dimensions).
    Changed: when an alpha dies, same-species mutants on the level hunt the killer.
    Added: artefact pickup triggers reactions. Outlaws chase the taker; aberrant mutants converge on the site.
    Changed: decay and cooldown timers use game time (respects time acceleration and realism mods).
  Core:
    Changed: pipeline redesign. Ineligible squads filtered before evaluation budget. Every trigger produces a result.
    Changed: cascading causes with pure predicate gates. Rejections skip expensive queries and immediately try the next cause.
    Added: squad ownership reassertion. Dead squad cleanup. Invalid cause-consequence pairs blocked at cause level.
    Added: statistics overlay. Real-time pipeline metrics (rates, blockers, counts), configurable position via MCM.
    Changed: map markers use blue icons, linger 60s after completion, then fade.
    Changed: general tuning (rush defaults, gulag duration, return-to-simulation timing).
  MCM:
    Added: instincts configuration. Per-instinct toggles, thresholds, and rush settings.
    Added: statistics overlay configuration.
    Added: per-consequence personality floor and ceiling sliders.
  Integration:
    Added: activity record API. record() and get_record() for external mods (e.g. warfare map tooltips).
    Added: PDA marker descriptions via register_descriptions().
    Changed: register_owner() supports override. Integration guide rewritten with current API and examples.
  Compatibility:
    Removed: modded exes requirement. Works with vanilla Anomaly 1.5.3, Demonized, Demonized MT, AOEngine. Tested with GAMMA, EFP, Zona.

1.3.6
  Summary: Hotfix, save compat
  Fixed: updated save migrations. Old scripted squad entries released and rebuilt on load.

1.3.5
  Summary: Alignment system, alpha mutants, reactive chaining, combat mutators, expanded mutant factions
  Gameplay:
    Added: alpha mutants. Mutants that rack up kills become alphas. They hit harder, absorb more punishment, never flee, and drop bonus loot for their species. Scales with kill level. Each feature toggleable in MCM.
    Added: alpha kill retaliation. Killing an alpha triggers same-species mutants to hunt the killer.
    Added: species-based squad death responses. Predator and territorial packs avenge killed members. Herbivores and weak mobs scatter.
    Added: combat mutators via hit callbacks. Alpha hit power scales with kill level. Veteran+ stalker hit power scales from veteran to legend rank. Separate MCM toggles.
    Added: alignment system based on GSC's moral axis (principled, self-serving, unprincipled, outlaw). Military never flees, ecologists never conquer, renegades never investigate etc
    Added: mutant territorial behavior. Mutants conquer territory, avenge their dead, and hunt wounded stalkers based on personality traits.
    Changed: reactive consequences chain. A single event now triggers multiple independent responses. A squad wipe fires both revenge and retreat. A medkit use attracts predators and sends allies to help. A massacre draws scavengers and investigators.
    Changed: personality probability narrowed. Factions commit to identity-consistent behavior more often and hesitate less.
    Changed: squads stay at destinations for realistic durations to let gulag jobs (patrol, sleep, socialize etc) unfold. Previously all squads left after 5 minutes.
    Removed: stalker elites. The engine tracks stalker progression natively through kill-to-rank points.
    Removed: elite kill bounty.
  Core:
    Added: mutators MCM section with toggles for alpha and stalker combat modifiers.
	Added: tracing statistics every minute when debug is on, will help standardise log pasing from users
  i18n:
    Added: full Russian description (doc/readme_ru.txt).
    Fixed: Russian MCM synced with English.
  Fixed:
    Fixed: crash on fast travel when a scripted squad dies offline and the engine recycles its entity ID.

1.3.0
  Summary: Personality system, mutant conquest, territory decay, round-robin dispatch, ownership registry, integration framework, fixes
  Requires xlibs update.

  Gameplay:
    Added: personality profiles for all stalker and mutant factions with trait-driven consequence selection.
    Added: persistent map markers for elite promotion. Marks the elite's squad on the PDA until death.
    Added: squad ownership registry. Alife mods register an ownership filter, AlifePlus skips squads they own. Warfare and BAO registered via legacy proxies.
    Added: integration API for external mods. Register custom causes and consequences without modifying AlifePlus code.
    Added: automatic save data cleanup on version upgrade. Orphaned squads released, stale keys removed.
    Added: conquered territory decays after configurable game hours if not reconquered.
    Added: per-smart cooldown on area cause. Same location cannot trigger conquest repeatedly.
    Changed: each needs consequence tied to specific personality traits. Hunger and sleep use survival, money uses greed, jobs use territory and perception, social uses relation.
    Changed: per-consequence rate budget tightened. The same consequence type cannot dominate a session.
    Changed: post-arrival wait uses game time. Advances during time skips, survives save and load.
    Changed: squads detected as arrived faster (scan interval reduced).
    Changed: stash consequences (loot, ambush, fill) harmonized into the same config-driven pattern as needs.
    Changed: smart terrain mutation and load restore unified into a single periodic update.
    Changed: radiant interval and global consequence budget defaults adjusted for less frequent, more varied activity.
    Removed: job_worship and job_exercise consequences, MCM entries, PDA messages, and item sections.
    Removed: per-consequence chance sliders. Replaced by personality system. PDA chance sliders remain.
  Compatibility:
    Fixed: warfare and BAO ownership filters gave false positives on squads without the expected field.
  Fixed:
    Fixed: area conquest MCM toggle had no effect on previously conquered smart terrains.
    Fixed: radiant causes did not fire on first session after game start.
    Fixed: needs thresholds broke on young saves with low game time.
  Internal:
    Changed: codebase restructured into core (pipeline) and ext (domain).
    Changed: chance gate removal eliminates per-consequence random rolls from every dispatch cycle.
    Changed: protection gate no longer allocates strings below DEBUG log level.
    Changed: producer luabind crossings reduced via global xlibs caching.
    Changed: pacer retries faster on protection reject instead of burning the full interval.
    Changed: radiant and reactive pipelines use independent Bresenham counters. Reactive events no longer biased by radiant traffic volume.
    Added: consumer statistics (PIPELINE, CAUSES, DISPATCH, LIFECYCLE) logged every 60s at DEBUG. Zero cost above DEBUG.
    Changed: alignment and personality checks moved from core consumer to ap_ext_util. Core pipeline has zero domain knowledge.
    Changed: PDA sender selection and message dispatch moved from core to ap_ext_news. Core has zero ext references.
    Changed: all consequence gates (alignment, personality) run inside observe() with full tid/path tracing.
    Changed: guard clause detector recognizes multi-line guards and counts guards throughout the function body.
    Changed: rate limiting reworked with sliding window counters and per-pipeline pacing.
  xlibs (requires xlibs update):
    Added: composite squad protection guard with shared commander context.
    Added: vertex-to-level cache with zero luabind on repeat lookups.
    Changed: scripted check uses engine fields only. Mod-specific fields handled by AP ownership registry.
    Removed: dead code in squad and smart terrain modules.

1.2.3
  Summary: Radiant/reactive split, NPC protection, global cooldown, PDA rebuild, map marker split, performance, fixes
  Core:
    Added: radiant/reactive event split, world-state changes and combat events rate-limited independently (no starvation)
    Added: global radiant cooldown prevents consequence bursts in busy areas (MCM configurable)
    Changed: map marker system decoupled from tracing and optimized for performance
    Changed: consequence chances use pseudo-random distribution for uniform distribution
  Gameplay:
    Fixed: territory changes hands only after the conquering squad physically arrives
    Fixed: stashes only filled when empty, prevents stacking
    Fixed: mutant squads no longer trigger stalker stash events, filtered at cause level
    Fixed: squads no longer churn between nearby locations, with fixed TTL between radiant transitions
    Fixed: revenge only rolls against valid enemy factions 
    Fixed: elite hunters respect the protection list 
    Fixed: stash loot evaluates squad eligibility before chance roll
    Changed: default consequence chances retuned (stash_fill 15%, stash/area/needs 20%, reactive 30%)
  Compatibility:
    Changed: four protection types (permanent, active role, task target, externally scripted) verified through one unified function ensuring single source of truth
    Changed: layered NPC protection: producer rejects radiant squads before any cause evaluates, consequence search excludes all four types, tracker rejects anything that reaches script_squad
    Changed: bounty targets, hostage targets, and externally scripted squads rejected at producer level
  Map & PDA:
    Added: map markers MCM toggle, independent from debug tracing
    Changed: PDA sender rebuilt to match dynamic_news_manager best practices (friendly factions only, online NPC registry)
    Changed: PDA messages come from stalkers already in the world (no server-object lookups, no sender cache)
  MCM:
    Added: version displayed at startup (mod + xlibs)
    Changed: MCM visual refresh
    Fixed: Russian MCM encoding restored (windows-1251)
  X-Ray & xlibs:
    Added: timed events work correctly with demonized_time_events and major modpacks (globals resolve at call time)
    Added: log deadbanding suppresses repeated lines in busy areas for cleaner logs
    Added: consequence rate limit peek without consuming tokens
    Added: cleaner console output (xmath.chance diagnostic noise removed)
    Changed: find_squads distance bounds use inclusive upper
	
1.2.2
  Summary: xlibs dependency update
  xlibs 1.2.3 dependency.
  Changed: xstash.loot_stash call updated (xlibs rename from loot_stash_to_npc)

1.2.1
  Summary: Russian translation
  Localization:
    Added: Russian MCM translation

1.2.0
  Summary: Needs system, item consumption, area conquest, stash protection, shelter routing, walk/rush toggle, fixes
  NPC protection:
    Fixed: named NPCs, traders, quest givers, and guards excluded from AlifePlus
    Fixed: squads already on a mission (Warfare patrols, vanilla scripted routes) are now skipped
    Fixed: guards and bodyguards added to protected list (Rostok, Yantar, Army Warehouses, Jupiter, Pripyat, Sarcophagus, Lab X-18)
    Fixed: Aslan (Dead City barman) ID corrected in protected list
    Fixed: squads no longer respond to their own events
  Gameplay:
    Added: walk/rush toggle per consequence (MCM per-consequence on/off)
    Added: stash protection - NPCs skip stashes containing toolkits and essential items (MCM on/off)
    Added: area conquest mutation toggle - keep squad movement without changing smart terrain ownership (MCM on/off)
    Added: shelter outdoor - squads seek campfires when no indoor shelter nearby (MCM on/off)
    Added: stash fill allowed at bases, ambush and loot still blocked
    Changed: revenge and elite hunters skip friendly factions
    Changed: area conquest only targets enemy or unclaimed territory
    Changed: needs back off after failing to find a destination, other needs get a turn
    Changed: default consequence chances raised to 30% (MCM per-consequence chance sliders)
    Note: needs use game time - thresholds work correctly at any time_factor including real-time
  Smart terrain routing:
    Changed: heal and shelter now target surge shelters, not mutant lairs
    Changed: heal, shelter, social accept any non-hostile location instead of requiring a base
    Changed: guard, worship, exercise send squads to outposts and forward positions, away from bases
    Changed: squads arriving at full locations complete their action then return to simulation
  Stability:
    Fixed: elite kill PDA always showing mutant template instead of stalker
    Fixed: consequence handlers duplicating across save/load cycles
    Fixed: artefact drop/pickup cycling triggering infinite harvest chasers
    Changed: squad tracking uses game time - survives sleep and time acceleration
    Changed: xlibs patch updates no longer require AlifePlus updates
  Internal:
    Changed: debug/trace overhead eliminated below DEBUG log level
    Added: structured arrival logging, smart search diagnostics
    Changed: territory conquest extracted to own module
    Removed: periodic sync (belt and suspenders, never needed), dead enum values

1.2.0-RC1 - "Needs"
  Summary: Needs system, radiant/reactive split, PDA messages, MCM overhaul, manifesto
  Needs:
    Added: 9 new causes/needs: hunger, sleep, rest, heal, shelter, money, supply, job, social
    Added: 15 new consequences with per-need MCM controls (enable, threshold, chance, action)
    Added: Night gate, faction-aware destination filter, destination limiter
    Added: 8 arrival actions: NPCs consume real inventory items or trade artefacts for supplies
    Added: Needs persisted per-squad in save data
  Event pipeline:
    Added: Radiant/reactive split with independent token buckets
    Added: Consequence token bucket replaces global cause lock
    Added: Radiant exhaustion peek, actor_on_interaction adapter, unscriptable guard
  PDA:
    Added: 168 message variants across 40 categories, stalker/mutant distinction
  MCM:
    Added: Reset button, retuned default chances, cleaned up labels
    Added: Russian translation (Stalker_Boss)
  Performance:
    Changed: MCM snapshot pattern, debug/trace function-swap (zero cost at WARN)
  Docs:
    Added: manifesto.md with GSC vision, engine source proof, developer quotes
  Fixed:
    Fixed: Reactive event starvation (cause lock starving needs and area causes)
    Fixed: wounded_help faction matching (actor_stalker prefix)
    Fixed: PDA messages now distinguish stalkers from mutants
    Fixed: supply_trader exchange giving free items (now requires artefact)

1.1.1
  Summary: fixes
  Fixed: dependency gate uses exact version match instead of string comparison

1.1.0
  Summary: Elite system, A-Life Ratio, grenade restock, AP ammo, rank boost, fixes
  10-level elite system replaces binary elite/legend. Alife Ratio balances simulation across maps.
  Elite: 10 levels, AP ammo restock (best k_ap for weapon), engine rank boost (up to 20% accuracy), grenade restock. Alife Ratio: Bresenham ratio gate for on/off-map event distribution, MCM slider (-10 to +10).
  Fixed: elitekill_targeted never firing (bounty was stopping the chain)
  Fixed: PDA message tone, elite buff performance

1.0.1
  Summary: Dependency gate, Russian translation, fixes
  Dependency gate, bugfixes, Russian translation.
  Added: dependency gate - clear error message if xlibs is missing or outdated
  Added: Russian translation (Stalker_Boss)
  Fixed: companion squads excluded from squad searches (xlibs)
  Fixed: player-created stashes excluded from stash searches (xlibs)

1.0.0
  Summary: First release, massacre, squad kill, base kill, wounded, harvest, stash, area conquer, chase API, MCM
  First release. Event-driven emergent A-Life system.
  10 causes, 17 consequences. Full MCM configuration.
  Added: reactive causes (massacre, squadkill, basekill, elite, legend,
         legendkill, wounded, harvest)
  Added: radiant causes (stash, area)
  Added: chase API, area conquest, hit modifiers, rank boost
