
next
  Fixed:
    Fixed: area_conquest_mutate_smart toggle had no effect on previously conquered smarts (old conquests restored every load regardless of setting)
  Pipeline:
    Changed: single squad_on_update tick source replaces enter/leave_smart dependency (no modded exe required)
    Changed: radiant pipeline gate chain: PACER(1/5) -> SCRIPTED(2/5) -> RATIO(3/5) -> PROTECTION(4/5) -> EVAL(5/5)
    Changed: reactive pipeline gate chain: PACER(1/3) -> RATIO(2/3) -> EVAL(3/3)
    Changed: all gates extracted into named local functions with cost/complexity/order/rationale headers
    Changed: uniform gate polarity (true=allowed, if-not chain)
    Added: _scripted_during_eval mid-chain guard prevents competing consequences from overriding each other's squad destinations
    Changed: radiant PACER uses os.clock timestamp comparison (default 5s, MCM 1-30s), rejects ~99%+ of squad_on_update calls
    Changed: reactive PACER renamed from _check_reactive_event_limiter to _reactive_pacer_gate
    Changed: on-map vs off-map detection uses squad.online field instead of level comparison (eliminates luabind call)
    Removed: global radiant cooldown MCM setting (redundant with PACER gate)
    Removed: _last_interaction_squad dedup cache (redundant with SCRIPTED gate)
    Removed: _radiant_event_limiter token bucket (replaced by timestamp PACER)
  Arrival:
    Changed: post_arrived_wait uses game time (advances during time skips, survives save/load)
    Changed: housekeeping arrival scan runs every 10s via CreateTimeEvent (was 30s)
    Changed: on_arrive registered via consumer.register opts (no separate register_arrival_handler call needed)
    Changed: consequence name IS the arrival handler key (one identity: trace key, rate limit key, arrival key)
    Changed: arrival keys use CONSEQUENCE constants (stash_loot -> CONSEQUENCE.STASH_LOOT, etc.)
    Changed: chase arrival logic inlined into each chase consequence (squadkill_revenge, elitekill_targeted, harvest_hunt)
    Removed: chase_register/chase_start from ap_utils (~115 lines)
    Fixed: shelter_outdoor missing from ap_test NEEDS_ARRIVE_KEYS
  Architecture:
    Added: ap_squad.script -- squad ownership, scripting lifecycle, arrival detection, protection, PDA markers
    Changed: ap_alife_tracker stripped to domain logic only (kills, elites, needs, death handler)
    Changed: is_protected moved from ap_utils to ap_squad
    Changed: scripted_squads, arrival handlers, consequence squads, marker sync extracted from tracker to ap_squad
    Changed: all module-local collections prefixed with _ (private convention)
    Changed: save/load split -- ap_squad saves to m_data.ap_core_squad, tracker saves to m_data.ap_ext_tracker
    Removed: _validate_script_target from script_squad (protection is upstream: pipeline gates + find_squads exclusions)
  Ownership:
    Added: squad ownership registry -- register_owner(name, filter_fn) / get_owner(squad) in ap_squad
    Added: proxy ownership filters for Warfare (registered_with_warfare) and BAO (__lock), registered at init
    Added: MCM toggle allow_external_ownership (default true)
    Changed: is_protected checks ownership registry before is_scripted fallback
    Changed: find_squads uses get_owner as exclude_filter (runs before luabind checks in xsquad)
  Constants:
    Changed: all inline magic strings replaced with ap_const constants (CALLBACK, REASON, TRACE, CAUSE) across 17 files
    Added: TRACE table in ap_const -- 13 diagnostic result codes for observe closures (OK, NO_SMART, CO_LOCATED, etc.)
  File structure:
    Changed: all files renamed to ap_core_* (framework) and ap_ext_* (domain extensions)
    Changed: ap_producer_reactive renamed to ap_core_producer
    Changed: ap_alife_tracker renamed to ap_ext_tracker
    Changed: all private locals/functions prefixed with _ across all files (21 renames)
    Changed: producer.unregister made local (never called externally)
    Added: ap_core_migration.script -- one-time save data migrations on load_state
    Changed: save keys renamed to match new module names (ap_core_squad, ap_ext_tracker, ap_core_smart_mutator)
    Added: migration v1.3.0 releases orphaned scripted squads, deletes all deprecated save keys (ap_squad, ap_alife_tracker, ap_smart_mutator, ap_conquest)
  Review fixes:
    Fixed: radiant causes dead on first session -- _rebuild_radiant_set ran before causes registered (alphabetical on_game_start order)
    Fixed: debug log level read MCM before config loaded (same alphabetical order issue)
    Fixed: warfare ownership filter ~= nil caught false as owned (false ~= nil is true in Lua)
    Fixed: bao ownership filter same ~= nil issue
    Changed: producer/debug init split into declare (on_game_start) + aggregate (actor_on_first_update)
    Changed: chase consequence move loops extracted to _move_actor_chasers/_move_smart_chasers (fixes 3-level nesting)
    Changed: debug args with xtable.count/tostring wrapped in enabled() guard (5 locations)
    Changed: arrival scan uses cached _pending_buf instead of per-call allocation
    Changed: needs _dispatch_arrive guard clause flattened
    Removed: unused cached globals (error, time_global, math_huge, ipairs, tostring, RESULT, REASON, CALLBACK, RegisterScriptCallback across 3 files)
  Markers:
    Changed: marker system redesigned -- one collection (_ap_marked_squads) with is_marked flag replaces two collections (_ap_consequence_squads + _ap_marked_squads)
    Changed: marker update split into _apply_markers (5s, flag-based) and _validate_markers (60s, entity checks)
    Changed: marker alive check reads entity directly (se.scripted_target) instead of _ap_scripted_squads lookup
    Added: persistent markers for non-movement consequences (elite_promote marks elite's squad until death)
    Added: add_marked_squad(squad_id, label, opts) replaces register_consequence_squad
    Changed: scripted squad lifecycle functions renamed (_update_scripted_squads, _update_scripted_squad, _update_arrival_flags, _update_wait)
    Fixed: logger prefix in producer registration errors ([PRODUCER] -> [PRODUCER.REACTIVE])
  Rate limiting:
    Added: global consequence rate limit (sliding window, cfg.global_consequence_max_events default 5/60s)
    Changed: consequence per-type rate limit uses token bucket (cfg.consequence_max_events default 2/60s)
    Removed: PRD (pseudo-random distribution) from all consequence chance rolls, replaced with plain xmath.chance
  Debugging:
    Added: community field to all consequence wrong_community trace returns
    Added: chance field to all consequence chance_next trace returns
    Added: PACER_SUMMARY periodic log (accepted/rejected counts per 30s window)
    Changed: name resolution uses section_name for blocked squads in protection gate log
  Compatibility:
    Added: ap_core_compat.script -- save data migrations + proxy ownership registrations (warfare, bao)
    Changed: warfare/bao proxy registrations moved from ap_core_squad to ap_core_compat (compatibility glue separate from core)
    Added: migration v1.4.0 migrates conquest save key after core->ext rename
  Style:
    Removed: === banner comments from all AP and xlibs files
  Protection:
    Changed: is_protected delegates to xsquad.is_protected (shared commander context, eliminates redundant resolution)
    Fixed: protection gate section_name() guarded behind enabled() (no string alloc at non-DEBUG log levels)
  Fixed:
    Fixed: needs scatter init clamp (math_max removed, negative timestamps valid for young saves)
    Fixed: PACER retries on protection reject (0.5s retry instead of burning full interval)
  xlibs (requires xlibs update):
    Added: xsquad.is_protected(squad, opts) composite guard with shared commander context
    Added: xlevel.get_level_id vertex-to-level cache (0 luabind on repeat lookups)
    Changed: is_externally_scripted renamed to is_scripted, drops __lock and registered_with_warfare (mod-specific fields handled by AP ownership registry)
    Changed: is_scripted checks engine/vanilla fields only (scripted_target, action_condlist, random_targets)
    Changed: exclude_externally_scripted renamed to exclude_scripted in find_squad/find_squads
    Removed: find_online_squads (dead code, zero callers)

1.2.3
  Core:
    Added: radiant/reactive event split, world-state changes and combat events rate-limited independently (no starvation)
    Added: global radiant cooldown prevents consequence bursts in busy areas (MCM configurable)
    Changed: map marker system decoupled from tracing and optimized for performance
    Changed: consequence chances use pseudo-random distribution for uniform distribution
  Gameplay:
    Fixed: territory changes hands only after the conquering squad physically arrives
    Fixed: stashes only filled when empty, prevents stacking
    Fixed: mutant squads no longer trigger stalker stash events, filtered at cause level
    Fixed: squads no longer churn between nearby locations, with fixed TTL between radiant transitions
    Fixed: revenge only rolls against valid enemy factions 
    Fixed: elite hunters respect the protection list 
    Fixed: stash loot evaluates squad eligibility before chance roll
    Changed: default consequence chances retuned (stash_fill 15%, stash/area/needs 20%, reactive 30%)
  Compatibility:
    Changed: four protection types (permanent, active role, task target, externally scripted) verified through one unified function ensuring single source of truth
    Changed: layered NPC protection: producer rejects radiant squads before any cause evaluates, consequence search excludes all four types, tracker rejects anything that reaches script_squad
    Changed: bounty targets, hostage targets, and externally scripted squads rejected at producer level
  Map & PDA:
    Added: map markers MCM toggle, independent from debug tracing
    Changed: PDA sender rebuilt to match dynamic_news_manager best practices (friendly factions only, online NPC registry)
    Changed: PDA messages come from stalkers already in the world (no server-object lookups, no sender cache)
  MCM:
    Added: version displayed at startup (mod + xlibs)
    Changed: MCM visual refresh
    Fixed: Russian MCM encoding restored (windows-1251)
  X-Ray & xlibs:
    Added: timed events work correctly with demonized_time_events and major modpacks (globals resolve at call time)
    Added: log deadbanding suppresses repeated lines in busy areas for cleaner logs
    Added: consequence rate limit peek without consuming tokens
    Added: cleaner console output (xmath.chance diagnostic noise removed)
    Changed: find_squads distance bounds use inclusive upper
	
1.2.2
  xlibs 1.2.3 dependency.
  Changed: xstash.loot_stash call updated (xlibs rename from loot_stash_to_npc)

1.2.1
  Localization:
    Added: Russian MCM translation

1.2.0
  NPC protection:
    Fixed: named NPCs, traders, quest givers, and guards excluded from AlifePlus
    Fixed: squads already on a mission (Warfare patrols, vanilla scripted routes) are now skipped
    Fixed: guards and bodyguards added to protected list (Rostok, Yantar, Army Warehouses, Jupiter, Pripyat, Sarcophagus, Lab X-18)
    Fixed: Aslan (Dead City barman) ID corrected in protected list
    Fixed: squads no longer respond to their own events
  Gameplay:
    Added: walk/rush toggle per consequence (MCM per-consequence on/off)
    Added: stash protection - NPCs skip stashes containing toolkits and essential items (MCM on/off)
    Added: area conquest mutation toggle - keep squad movement without changing smart terrain ownership (MCM on/off)
    Added: shelter outdoor - squads seek campfires when no indoor shelter nearby (MCM on/off)
    Added: stash fill allowed at bases, ambush and loot still blocked
    Changed: revenge and elite hunters skip friendly factions
    Changed: area conquest only targets enemy or unclaimed territory
    Changed: needs back off after failing to find a destination, other needs get a turn
    Changed: default consequence chances raised to 30% (MCM per-consequence chance sliders)
    Note: needs use game time - thresholds work correctly at any time_factor including real-time
  Smart terrain routing:
    Changed: heal and shelter now target surge shelters, not mutant lairs
    Changed: heal, shelter, social accept any non-hostile location instead of requiring a base
    Changed: guard, worship, exercise send squads to outposts and forward positions, away from bases
    Changed: squads arriving at full locations complete their action then return to simulation
  Stability:
    Fixed: elite kill PDA always showing mutant template instead of stalker
    Fixed: consequence handlers duplicating across save/load cycles
    Fixed: artefact drop/pickup cycling triggering infinite harvest chasers
    Changed: squad tracking uses game time - survives sleep and time acceleration
    Changed: xlibs patch updates no longer require AlifePlus updates
  Internal:
    Changed: debug/trace overhead eliminated below DEBUG log level
    Added: structured arrival logging, smart search diagnostics
    Changed: territory conquest extracted to own module
    Removed: periodic sync (belt and suspenders, never needed), dead enum values

1.2.0-RC1 - "Needs"
  Needs:
    Added: 9 new causes/needs: hunger, sleep, rest, heal, shelter, money, supply, job, social
    Added: 15 new consequences with per-need MCM controls (enable, threshold, chance, action)
    Added: Night gate, faction-aware destination filter, destination limiter
    Added: 8 arrival actions: NPCs consume real inventory items or trade artefacts for supplies
    Added: Needs persisted per-squad in save data
  Event pipeline:
    Added: Radiant/reactive split with independent token buckets
    Added: Consequence token bucket replaces global cause lock
    Added: Radiant exhaustion peek, actor_on_interaction adapter, unscriptable guard
  PDA:
    Added: 168 message variants across 40 categories, stalker/mutant distinction
  MCM:
    Added: Reset button, retuned default chances, cleaned up labels
    Added: Russian translation (Stalker_Boss)
  Performance:
    Changed: MCM snapshot pattern, debug/trace function-swap (zero cost at WARN)
  Docs:
    Added: manifesto.md with GSC vision, engine source proof, developer quotes
  Fixed:
    Fixed: Reactive event starvation (cause lock starving needs and area causes)
    Fixed: wounded_help faction matching (actor_stalker prefix)
    Fixed: PDA messages now distinguish stalkers from mutants
    Fixed: supply_trader exchange giving free items (now requires artefact)

1.1.1
  Fixed: dependency gate uses exact version match instead of string comparison

1.1.0
  10-level elite system replaces binary elite/legend. Alife Ratio balances simulation across maps.
  Elite: 10 levels, AP ammo restock (best k_ap for weapon), engine rank boost (up to 20% accuracy), grenade restock. Alife Ratio: Bresenham ratio gate for on/off-map event distribution, MCM slider (-10 to +10).
  Fixed: elitekill_targeted never firing (bounty was stopping the chain)
  Fixed: PDA message tone, elite buff performance

1.0.1
  Dependency gate, bugfixes, Russian translation.
  Added: dependency gate - clear error message if xlibs is missing or outdated
  Added: Russian translation (Stalker_Boss)
  Fixed: companion squads excluded from squad searches (xlibs)
  Fixed: player-created stashes excluded from stash searches (xlibs)

1.0.0
  First release. Event-driven emergent A-Life system.
  10 causes, 17 consequences. Full MCM configuration.
  Added: reactive causes (massacre, squadkill, basekill, elite, legend,
         legendkill, wounded, harvest)
  Added: radiant causes (stash, area)
  Added: chase API, area conquest, hit modifiers, rank boost
